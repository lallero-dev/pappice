name: pappice

services:
  pappice:
    build:
      context: ../..
      dockerfile: Dockerfile
    image: pappice:local
    restart: unless-stopped
    env_file:
      - ./pappice.env
    ports:
      - "127.0.0.1:8388:8388"
    volumes:
      - ./data:/data
      - ./backups:/backups
